# ============================================================
# ELF2 绝缘子缺陷检测系统 - 依赖清单
# ============================================================
# 安装方式:
#   pip install -r requirements.txt
#
# RK3588 板端额外依赖 (手动安装 .whl):
#   rknn_toolkit_lite2  (从瑞芯微官方获取 rknn_toolkit_lite2-*-cp310-cp310-linux_aarch64.whl)
# ============================================================

# ---- 核心依赖 ----
numpy>=1.22.0,<2.0
opencv-python>=4.6.0,<5.0
Pillow>=9.0.0

# ---- 板端推理 (手动安装, 此处仅注释) ----
# rknn_toolkit_lite2  # RK3588 NPU 推理, 需从瑞芯微官方获取 .whl

# ---- PC 端训练/评估 ----
torch>=2.0.0
ultralytics>=8.0.190

# ---- ONNX 转换 ----
onnx>=1.14.0
onnxruntime>=1.15.0
onnx-simplifier>=0.4.0

# ---- Qt 触屏界面 (板端/Python 3.10) ----
PyQt5>=5.15.0

# ---- API 核验 ----
requests>=2.28.0

# ---- GPIO 告警 (板端) ----
# RPi.GPIO  # 树莓派 / RK3588 GPIO 控制

# ---- 可选: 数据增强 ----
# albumentations>=1.3.0

# ---- 可选: 仪表盘 Web 界面 ----
flask>=3.0.0
